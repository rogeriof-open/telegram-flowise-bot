# Bot do Telegram
python-telegram-bot==20.3

# Requisições HTTP para integração com Flowise
httpx==0.27.0

# Gerenciamento de variáveis de ambiente (.env)
python-dotenv==1.0.1

# Logs estruturados (opcional, mas recomendado)
loguru==0.7.2

# Manipulação de JSON avançada (opcional, se precisar)
ujson==5.10.0
